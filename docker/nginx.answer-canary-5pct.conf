split_clients "$http_authorization" $go_answer_cohort {
    5% go;
    * fastapi;
}

map $request_uri $go_answer_canary {
    default fastapi;
    ~^/api/exams/submit-answer(\?|$) $go_answer_cohort;
}

map $go_answer_canary $answer_backend {
    default fastapi_backend;
    go go_start_backend;
}
