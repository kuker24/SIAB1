# All START traffic to Go. FastAPI remains the upstream backup only.
map $request_uri $go_start_canary {
    default fastapi;
    ~^/api/exams/[0-9]+/start$ go;
}

map $go_start_canary $start_backend {
    default fastapi_backend;
    go go_start_backend;
}
