split_clients "$http_authorization" $go_submit_cohort {
    25% go;
    * fastapi;
}

map $request_uri $go_submit_canary {
    default fastapi;
    ~^/api/exams/submit(\?|$) $go_submit_cohort;
}

map $go_submit_canary $submit_backend {
    default fastapi_backend;
    go go_start_backend;
}
