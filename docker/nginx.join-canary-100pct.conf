# All JOIN traffic to Go. FastAPI remains the upstream backup only.
map $request_uri $go_join_canary {
    default fastapi;
    ~^/api/exams/join(\?|$) go;
}

map $go_join_canary $join_backend {
    default fastapi_backend;
    go go_start_backend;
}
