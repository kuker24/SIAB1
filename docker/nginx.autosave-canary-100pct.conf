map $request_uri $go_autosave_canary {
    default fastapi;
    ~^/api/exams/auto-save(\?|$) go;
}

map $go_autosave_canary $autosave_backend {
    default fastapi_backend;
    go go_start_backend;
}
