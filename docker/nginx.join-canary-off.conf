# Fail-safe default: every JOIN request stays on FastAPI.
map $request_uri $go_join_canary {
    default fastapi;
}

map $go_join_canary $join_backend {
    default fastapi_backend;
    go go_start_backend;
}
