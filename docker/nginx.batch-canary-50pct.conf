split_clients "$http_authorization" $go_batch_cohort {
    50% go;
    * fastapi;
}

map $request_uri $go_batch_canary {
    default fastapi;
    ~^/api/exams/auto-save-batch(\?|$) $go_batch_cohort;
}

map $go_batch_canary $batch_backend {
    default fastapi_backend;
    go go_start_backend;
}
