# Fail-safe default: every START request stays on FastAPI.
map $request_uri $go_start_canary {
    default fastapi;
}

map $go_start_canary $start_backend {
    default fastapi_backend;
    go go_start_backend;
}
